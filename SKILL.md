---
name: household-report-update
description: 이관범 가계부 CSV를 받아 리포트를 재계산하고 GitHub Pages(leekwanbeom.github.io/household-report)에 배포한다. 사용자가 "가계부 업데이트", "가계부 리포트", "가계부 CSV 넣어줘", "가계부 배포", "household report", "가계부 갱신", "리포트 다시 만들어줘" 같은 말을 하거나, 가계부로 보이는 CSV(날짜/구분/대분류/소분류/세부내용/금액/결제수단/고정여부/비고 컬럼)를 업로드하면 반드시 이 스킬을 사용할 것. 가계부 데이터 분석·계좌 잔고 계산·고정지출 예측·신용카드 실적 확인 요청에도 적용된다.
---

# 가계부 리포트 업데이트

이관범님의 가계부 CSV를 받아 리포트를 재계산하고 GitHub Pages에 배포한다.

빌드 코드(`pipeline.py`, `build.py`, `scripts/`)는 이 저장소에 있다. 컨테이너가
초기화되어도 매번 저장소에서 최신본을 받아오므로 로컬 상태에 의존하지 않는다.

## 작업 순서

### 1. 준비 — 저장소에서 코드 받기

작업 디렉토리는 항상 `/home/claude/` 하위를 쓴다.

```bash
mkdir -p /home/claude/gagyebu && cd /home/claude/gagyebu
curl -sL https://codeload.github.com/LeeKwanBeom/household-report/tar.gz/refs/heads/main | tar xz --strip-components=1
ls
```

`pipeline.py`, `build.py`, `scripts/`가 다 보이면 성공이다. 하나라도 없으면
진행하지 말고 사용자에게 알린다.

**`raw.githubusercontent.com`으로 개별 파일을 받지 말 것.** 5분간 캐시되어
push 직후에 받으면 옛 파일이 내려오고, `scripts/`가 통째로 빠진다.

### 2. CSV 검증

업로드된 CSV를 `/mnt/user-data/uploads/`에서 찾아 작업 디렉토리로 복사한다.
경로를 추측하지 말고 `view /mnt/user-data/uploads`로 실제 파일명을 확인할 것.

pandas로 직접 읽어서 컬럼을 검증한다. 수동 하드코딩은 절대 하지 않는다.

```python
import pandas as pd
df = pd.read_csv(path, encoding='utf-8-sig', low_memory=False)
cols = [c.strip() for c in df.columns]
```

기대 컬럼: `날짜, 구분, 대분류, 소분류, 세부내용, 금액, 결제수단, 고정여부, 비고`

컬럼이 다르면 무엇이 빠졌는지 알리고 중단한다.

`결제수단`에는 **계좌명**이 들어가야 한다(`생활비`/`신한은행`/`청년미래적금`/`대여금`).
카드명은 `비고`에 넣는다. 여기가 바뀌면 그 거래가 잔고 계산에서 조용히 빠진다.

### 3. 이체 건 확인 (조건부 질문)

CSV에서 `구분=="이체"` 행을 찾는다.

**이체 건이 있으면** — 물어보지 말고 확인 결과만 보고하고 다음 단계로 진행한다:

> 이체 건 확인됐어요: 신한은행 525,000원 · 청년미래적금 500,000원

**이체 건이 없거나 한쪽만 있으면** — 여기서 멈추고 사용자에게 묻는다:

> 이번 CSV에 [청년미래적금] 이체 건이 안 보여요. 빠뜨리신 걸까요?
>
> 빠뜨리신 거라면 이 줄을 추가해서 다시 올려주세요:
>
> | 날짜 | 구분 | 대분류 | 소분류 | 세부내용 | 금액 | 결제수단 | 고정여부 | 비고 |
> |---|---|---|---|---|---|---|---|---|
> | 2026-10-01 | 이체 | 이체 | 청년미래적금 | 적금 이체 | 500,000 | 생활비 | 고정 | |

**사용자 답변을 받기 전에 다음 단계로 넘어가지 않는다.** 이 확인이 중요한 이유는,
이체가 누락되면 계좌 잔고가 실제와 어긋난 채로 배포되기 때문이다.
`pipeline.py`에는 이체 누락을 잡는 검증이 없다. 이 확인이 유일한 방어선이다.

### 4. 시작 잔고 확인

`pipeline.py`의 `ACCOUNTS_START`에 적힌 현재 시작 잔고를 사용자에게 보여주고
그대로 쓸지 확인한다. 값은 코드에서 읽어서 보여주고, 여기 적힌 숫자를 옮겨
적지 않는다.

> 계좌 시작 잔고가 아래와 같이 설정돼 있어요. 그대로 진행할까요?
> - 생활비 통장: 3,342,548원 (2026-09-01 기준)
> - 신한은행 통장: 0원 (2026-09-01 기준)
> - 청년미래적금: 1,500,000원 (2026-09-03 기준)
> - 대여금: 3,300,000원 (2026-09-01 기준)

바뀐 값이 있으면 `pipeline.py`의 `ACCOUNTS_START`를 수정한 뒤 진행한다.

**`balance`와 `as_of`는 반드시 같이 갱신한다.** 잔고 계산은 계좌마다
`as_of` **다음날부터**의 거래만 더한다. 한쪽만 바꾸면 그 사이 거래가 통째로
사라지거나 두 번 세어진다. 이중계산 방지 규칙은 이 한 줄이 전부이며,
예외 목록은 따로 없다.

### 5. 재계산 및 빌드

```bash
cd /home/claude/gagyebu
python3 pipeline.py <csv파일명>
python3 build.py
```

`pipeline.py`는 집계 후 두 가지를 출력한다.

- **정합성 검증** — 실패하면 어디가 얼마나 어긋났는지 출력하고 중단한다.
  실패 시 그 내용을 사용자에게 그대로 전달한다.
- **경고** — 중단시키지는 않지만 조용히 틀린 값을 만드는 상황이다.
  계좌에 안 붙는 결제수단, 데이터에서 한 번도 안 걸린 제외 규칙 키,
  기준일 이후 거래가 없는 계좌, 실적이 0인 카드, 신한 고정비 목록에 없는 출금.
  **경고는 리포트 화면에 나오지 않으므로 반드시 대화로 전달한다.**

`build.py`는 `/mnt/user-data/outputs/이관범_가계부_분석_리포트.html`을 생성한다.

### 6. 종료 추정 항목 보고 (화면 아닌 대화로)

```bash
python3 scripts/detect_ended.py <csv파일명>
```

최근 2개월 이상 결제가 없는 '고정' 항목을 찾는다. 제외 목록은 `pipeline.py`에서
import하므로 따로 관리하지 않는다.

출력은 두 덩어리다. **`[확인 필요]`만 사용자에게 전달한다.**
`[이미 제외 처리됨]`은 조치가 필요 없다.

> 참고로 최근 결제가 끊긴 고정 항목이 있어요. 실제로 종료된 건지 확인해보세요:
> - 문화생활비 / 어플·멤버쉽 / OOO: 마지막 2026-05 (3개월 공백)

종료가 확인되면 `pipeline.py`의 `ENDED_FIXED_ITEMS`에 추가한다.
**리포트 화면에는 이 내용을 넣지 않는다** — 사용자가 명시적으로 화면에서
빼달라고 요청한 섹션이다.

### 7. 배포

배포는 사용자가 제공한 GitHub 토큰으로 진행한다. **토큰을 이 파일이나
스크립트에 저장하지 않는다.** 매번 사용자에게 요청한다.

토큰이 아직 없으면:

> 배포하려면 GitHub 토큰이 필요해요. 대화창에 붙여넣어 주세요.
> (Settings → Developer settings → Fine-grained tokens, household-report 저장소에
> Contents: Read and write 권한)

```bash
python3 scripts/deploy.py <html경로> <토큰>              # 리포트만
python3 scripts/deploy.py <html경로> <토큰> --with-code   # 리포트 + 코드
python3 scripts/deploy.py <토큰> --code-only             # 코드만 (화면 유지)
```

`pipeline.py`나 `build.py`를 고쳤으면 `--with-code`로 함께 올려야 다음 실행에
반영된다. 안 올리면 다음 세션에서 옛 코드를 받아온다.

배포 후 URL을 안내한다:

> 배포 완료했어요. https://leekwanbeom.github.io/household-report/
> (반영까지 1~2분 걸릴 수 있어요)

---

## 계산 규칙

이 규칙들은 `pipeline.py`의 `RULES` 섹션에 코드로 명시되어 있다. 규칙을 바꿔야 하면
`pipeline.py`를 수정하고, `--with-code`로 저장소에 다시 올린다.

### 이체 처리

`구분=="이체"` 건은 **총수입/총지출 집계에서 제외**하고 계좌 잔고 계산에만 반영한다.
이체는 수입도 지출도 아니라 통장 간 이동이기 때문이다.

### 이중 계산 방지

계좌마다 `as_of`가 있고, 잔고 계산은 **`as_of` 다음날부터**의 거래만 더한다
(`날짜 > as_of`). `as_of` 당일까지의 거래는 이미 시작 잔고 숫자 안에 녹아 있다.

예외 목록은 없다. 새 계좌가 추가되거나 잔고를 다시 잴 때도 `balance`와 `as_of`를
같이 갱신하는 것으로 끝난다.

### 매달 예상 고정지출

- 가장 최신 월은 **막날 데이터가 있어도 무조건 미완성**으로 간주해 제외하고,
  완성된 달만 계산한다
- 섹션 제목·note·KPI 라벨·표 헤더는 **특정 월을 지칭하지 않는 일반 문구**로 유지하고,
  완성된 달 수만 매번 갱신한다 (예: "매달 예상 고정지출", "1~8월 총액 ÷ 8개월 기준")
- 미완성 월도 **다른 섹션**(월평균·비중·히트맵 등)에서는 완성된 달과 동일하게 포함한다

### 고정여부

- 원본의 `'고정'` 값을 **그대로 유지**한다. 과거 데이터를 수정하지 않는다
- 최근 **2개월 이상 데이터가 없는 '고정' 항목**은 "종료 추정"으로 플래그한다
- `"어플/멤버쉽"`처럼 여러 구독이 한 소분류에 섞인 경우, **세부내용 단위**로 추가 확인한다

### 예상 고정지출에서 영구 제외

종료가 확인된 항목들이다 (`ENDED_FIXED_ITEMS`, 7건):
- 금융 / 회생
- 주거생활비 / OTT
- 문화생활비 / 어플·멤버쉽 / 리모트뷰
- 문화생활비 / 어플·멤버쉽 / Chat GPT
- 문화생활비 / 어플·멤버쉽 / MIB
- 문화생활비 / 어플·멤버쉽 / 삼성 케어
- 문화생활비 / 어플·멤버쉽 / 이모티콘 (월구독 → 1년권 전환으로 종료)

사용자 요청으로 제외 (`MANUALLY_EXCLUDED_FIXED_ITEMS`):
- 문화생활비 / 어플·멤버쉽 / 이모티콘 1년 (연간결제성)

**카카오 톡서랍은 제외 대상이 아니다.** 현재도 쓰는 항목이라
`SHINHAN_FIXED_ITEMS`에 월 990원으로 들어 있다. 이모티콘과 혼동하지 말 것.

### 정렬

- **03번 카드사별 결제 내역**: 거래 목록은 **날짜 오름차순**(오래된 날짜가 위)

### 기타

- `"이모티콘 월구독"`과 `"이모티콘"`은 같은 구독이므로 하나로 통합 (`SUBSCRIPTION_ALIASES`)
- **TOP 15**에서 회생·가족 용돈·커플통장은 제외 (매달 반복되는 큰 금액이라 순위 독점)

---

## 리포트 구조 (14개 섹션)

화면 번호는 `build.py`의 `renumber_sections()`가 등장 순서대로 다시 매긴다.
HTML의 `<section id>`는 개편 이력 때문에 번호와 일치하지 않는다 — id를 보고
번호를 추측하지 말 것.

| # | 섹션 | id | 그룹 |
|---|---|---|---|
| 01 | 계좌 잔고 | s1 | 자산 현황 |
| 02 | 신용카드별 실적 체크 | s2 | 이번 달 |
| 03 | 카드사별 결제 내역 | s3 | 이번 달 |
| 04 | 월별 수입·지출 추이 | s5 | 전체 분석 |
| 05 | 대분류별 지출 비중·소분류 상세 | s6 | 전체 분석 |
| 06 | 전월 대비 증감 | s-mom | 전체 분석 |
| 07 | 고정 지출 vs 변동 지출 | s7 | 전체 분석 |
| 08 | 카테고리 × 월 히트맵 | s9 | 전체 분석 |
| 09 | 최대 지출 TOP 15 | s10 | 전체 분석 |
| 10 | 신한은행 고정지출 예상 vs 실제 | s4 | 예측·목표 |
| 11 | 매달 예상 고정지출 | s11 | 예측·목표 |
| 12 | 핵심 요약 | s12 | 요약 |
| 13 | 확인이 필요한 항목 | s13 | 요약 |
| 14 | 월별 전체 내역 | s14 | 원장 |

"매달 목표 대비 실적"은 별도 섹션이 아니라 11번 안의 하위 블록(`h3`)이다.

섹션을 추가·삭제·재배치하면 `section-num` 값은 손대지 않아도 된다.
본문에서 다른 섹션을 가리킬 때는 숫자를 적지 말고 `{REF:섹션id}`를 쓴다.
수정한 `build.py`는 `--with-code`로 저장소에 올려야 다음 실행에 반영된다.

---

## 주의사항

- **로컬 경로를 하드코딩하지 않는다.** CSV는 항상 대화창 업로드로 받고,
  작업은 `/home/claude/` 하위에서만 한다
- **토큰을 파일에 저장하지 않는다.** 매번 사용자에게 받는다
- **데이터는 pandas로 직접 읽는다.** 숫자를 코드에 손으로 적어 넣지 않는다
- **규칙 값을 화면 문구에 옮겨 적지 않는다.** `pipeline.py`의 상수가 유일한
  원본이고, bundle을 통해 `build.py`로 전달된다. 문구에 직접 적으면 규칙을
  바꿔도 화면만 옛 값으로 남는다
- 배포 전 검증은 `scripts/deploy.py`가 자동으로 한다(임베드 JSON 파싱,
  `node --check`로 script 블록 문법, 치환 안 된 자리표시자). 별도로 할 일은 없다
